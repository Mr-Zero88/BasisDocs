---
title: Configuratie
---

import { Callout } from 'fumadocs-ui/components/callout';

{/*
## Introductie

De focus hier ligt op Basis Server-specifieke configuratie en niet op de vele manieren waarop je gameservers in de cloud kunt hosten.

...
*/}

### Compileren en uitvoeren op Linux

#### Vereisten voor Debian/Ubuntu

Voor Ubuntu 22.04,
`sudo add-apt-repository ppa:dotnet/backports`

Voor Debian of andere eigenzinnige setups:

```
wget https://packages.microsoft.com/config/debian/12/packages-microsoft-prod.deb -O packages-microsoft-prod.deb
sudo dpkg -i packages-microsoft-prod.deb
rm packages-microsoft-prod.deb
```

Ga daarna verder met de installatie:

`sudo apt-get update && sudo apt-get install -y dotnet-sdk-10.0`

#### Basis ophalen

Navigeer naar de map waarnaar je Basis wilt downloaden en voer de volgende opdracht uit:

`git clone -b long-term-support-20260425 https://github.com/BasisVR/Basis`

#### Basis Server bouwen

Open een nieuwe terminal en cd naar de map met de Basis Server, bijv. `cd Basis/Basis\ Server`

`dotnet restore`

Bouw daarna met:

`dotnet build` (voor debug)

of

`dotnet build --configuration Release` (voor release)

#### Uitvoeren

Navigeer naar de BasisServerConsole-map (zoiets als `/BasisServerConsole/bin/Debug/net10.0/BasisNetworkConsole`)
en voer uit:

`dotnet .\BasisNetworkConsole.dll`

## Firewall

Mogelijk wil je de volgende poorten openen:
 * 1234/tcp
 * 10666/tcp
 * 4296/udp

## Configuratiebestanden

De eerste keer dat je de server uitvoert, worden er verschillende configuratiebestanden en een structuur aangemaakt in de map waar de server exe zich bevindt.

| Bestand/Map              | Beschrijving                                |
|--------------------------|---------------------------------------------|
| `config.xml`             | Hoofdconfiguratiebestand voor de server.    |
| `initialresources/`      | Deze map bevat wereld- en objectbestanden die de server op verzoek laadt. |
| `Log/`                   | Map waar serverlogboeken worden opgeslagen. |
| `admins.xml`             | Lijst van gebruikers met beheerdersrechten. |
| `banned_players.xml`     | Lijst van verbannen spelersaccounts.        |

## Config.xml

| Variabele | Standaard | Beschrijving |
|---|---|---|
| `PeerLimit` | 65535 | Maximum aantal verbonden peers. |
| `SetPort` | 4296 | UDP-poort waarop de server luistert. |
| `UseNativeSockets` | true | Gebruikt native socket-aanroepen van het besturingssysteem in plaats van het managed pad. |
| `NatPunchEnabled` | false | Schakelt NAT punch-through in voor peer-introductie. |
| `PingInterval` | 1500 | Interval tussen keep-alive-pings, in milliseconden. |
| `DisconnectTimeout` | 30000 | Tijd zonder antwoord voordat een peer wordt verbroken, in milliseconden. |
| `SimulatePacketLoss` | false | Laat uitgaande pakketten kunstmatig vallen voor tests. |
| `SimulateLatency` | false | Vertraagt pakketten kunstmatig voor tests. |
| `SimulationPacketLossChance` | 10 | Pakketverliespercentage wanneer simulatie actief is. |
| `SimulationMinLatency` | 50 | Minimale extra latentie, in milliseconden. |
| `SimulationMaxLatency` | 150 | Maximale extra latentie, in milliseconden. |
| `ReconnectDelay` | 500 | Tijd voordat opnieuw verbinden wordt geprobeerd. |
| `MaxConnectAttempts` | 10 | Maximum aantal verbindingspogingen. |
| `ReuseAddresss` | false | Socket-hergebruikoptie; let op de spelling van het veld. |
| `DontRoute` | false | Socket-routeringsoptie. |
| `EnableStatistics` | true | Verzamelt transportstatistieken voor health- en statusrapportage. |
| `IPv6Enabled` | true | Schakelt IPv6 dual-stack socketondersteuning in. |
| `MtuOverride` | 0 | Forceert een vaste MTU; 0 schakelt overschrijven uit. |
| `MtuDiscovery` | true | Schakelt Path-MTU-detectie in. |
| `DisconnectOnUnreachable` | false | Verbreekt bij ICMP unreachable. |
| `AllowPeerAddressChange` | true | Staat toe dat een peer-endpoint tijdens de sessie wijzigt. |
| `HasFileSupport` | true | Schakelt schrijven van logs, moderatielijsten, auth-identiteitsdata en andere schijfdata in. |
| `HealthCheckHost` | localhost | Host of interface voor health checks. Gebruik * om aan alle interfaces te binden. |
| `HealthCheckPort` | 10666 | TCP-poort voor health checks. |
| `HealthPath` | /health | Pad naar het health-check-endpoint. |
| `BSRSMillisecondDefaultInterval` | 50 | Basis verzendinterval van het Server Reduction System, in milliseconden. |
| `BSRBaseMultiplier` | 1 | Basismultiplier voor afstandsschaling. |
| `BSRSIncreaseRate` | 0.005 | Hoe snel verzendintervallen groeien met afstand. |
| `BSRSlowestSendRate` | 2.55 | Langzaamste verzendsnelheid voor verre peers. |
| `HighQualityDistance` | 3 | Afstandsdrempel voor avatar-sync met hoge kwaliteit. |
| `MediumQualityDistance` | 10 | Afstandsdrempel voor avatar-sync met gemiddelde kwaliteit. |
| `LowQualityDistance` | 20 | Afstandsdrempel voor avatar-sync met lage kwaliteit. |
| `OverrideAutoDiscoveryOfIpv` | false | Bindt exact aan de geconfigureerde adressen in plaats van IP-versie automatisch te detecteren. |
| `IPv4Address` | 0.0.0.0 | IPv4-bindadres. |
| `IPv6Address` | :: | IPv6-bindadres. |
| `Password` | default_password | Serverwachtwoord. |
| `UseAuth` | true | Schakelt wachtwoordauthenticatie in of uit. |
| `UseAuthIdentity` | true | Vereist DID-identiteitscontrole wanneer auth actief is. |
| `BasisUserRestrictionMode` | Normal | Toegangsrestrictiemodus: Normal, BlackList of WhiteList. |
| `HowManyDuplicateAuthCanExist` | 2 | Aantal gelijktijdige verbindingen voor dezelfde auth-identiteit. |
| `AuthValidationTimeOutMiliseconds` | 9000 | Timeout voor auth-validatie, in milliseconden. |
| `EnableConsole` | true | Schakelt de interactieve serverconsole in. |
| `DisableWriteUnlessAdminPersistentFlag` | true | Weigert persistente key/value-schrijfacties tenzij de aanroeper admin is. |
| `DisableReadUnlessAdminPersistentFlag` | false | Weigert persistente key/value-leesacties tenzij de aanroeper admin is. |
| `UseNetworkFinalCompression` | false | Schakelt de finale netwerkcompressiestap in. |
| `EnableBSRProfiling` | false | Geeft profiling-output van het Server Reduction System. |
| `DisallowHeadless` | false | Weigert headless clients bij verbinden. |
| `AvatarsLocked` | false | Blokkeert avatar laden voor gebruikers zonder bypass-permissie. |
| `PropsLocked` | false | Blokkeert prop laden voor gebruikers zonder bypass-permissie. |
| `WorldsLocked` | true | Blokkeert wereld laden voor gebruikers zonder bypass-permissie. |

<small>Elke omgevingsvariabele waarvan de naam overeenkomt met een openbaar configuratieveld overschrijft dat veld bij het starten.</small>

### Authenticatie-instellingen

```xml
<UseAuth>true</UseAuth>
```

```xml
<UseAuthIdentity>true</UseAuthIdentity>
```

```xml
<BasisUserRestrictionMode>Normal</BasisUserRestrictionMode>
```

```xml
<HowManyDuplicateAuthCanExist>2</HowManyDuplicateAuthCanExist>
```

### Voorbeeld Config.xml

De standaardinstellingen die je in config.xml kunt vinden:

```xml title="config.xml"
<?xml version="1.0" encoding="utf-8"?>
<Configuration xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema">
  <PeerLimit>65535</PeerLimit>
  <SetPort>4296</SetPort>
  <UseNativeSockets>true</UseNativeSockets>
  <NatPunchEnabled>false</NatPunchEnabled>
  <PingInterval>1500</PingInterval>
  <DisconnectTimeout>30000</DisconnectTimeout>
  <SimulatePacketLoss>false</SimulatePacketLoss>
  <SimulateLatency>false</SimulateLatency>
  <SimulationPacketLossChance>10</SimulationPacketLossChance>
  <SimulationMinLatency>50</SimulationMinLatency>
  <SimulationMaxLatency>150</SimulationMaxLatency>
  <ReconnectDelay>500</ReconnectDelay>
  <MaxConnectAttempts>10</MaxConnectAttempts>
  <ReuseAddresss>false</ReuseAddresss>
  <DontRoute>false</DontRoute>
  <EnableStatistics>true</EnableStatistics>
  <IPv6Enabled>true</IPv6Enabled>
  <MtuOverride>0</MtuOverride>
  <MtuDiscovery>true</MtuDiscovery>
  <DisconnectOnUnreachable>false</DisconnectOnUnreachable>
  <AllowPeerAddressChange>true</AllowPeerAddressChange>
  <HasFileSupport>true</HasFileSupport>
  <HealthCheckHost>localhost</HealthCheckHost>
  <HealthCheckPort>10666</HealthCheckPort>
  <HealthPath>/health</HealthPath>
  <BSRSMillisecondDefaultInterval>50</BSRSMillisecondDefaultInterval>
  <BSRBaseMultiplier>1</BSRBaseMultiplier>
  <BSRSIncreaseRate>0.005</BSRSIncreaseRate>
  <BSRSlowestSendRate>2.55</BSRSlowestSendRate>
  <HighQualityDistance>3</HighQualityDistance>
  <MediumQualityDistance>10</MediumQualityDistance>
  <LowQualityDistance>20</LowQualityDistance>
  <OverrideAutoDiscoveryOfIpv>false</OverrideAutoDiscoveryOfIpv>
  <IPv4Address>0.0.0.0</IPv4Address>
  <IPv6Address>::</IPv6Address>
  <Password>default_password</Password>
  <UseAuth>true</UseAuth>
  <UseAuthIdentity>true</UseAuthIdentity>
  <BasisUserRestrictionMode>Normal</BasisUserRestrictionMode>
  <HowManyDuplicateAuthCanExist>2</HowManyDuplicateAuthCanExist>
  <AuthValidationTimeOutMiliseconds>9000</AuthValidationTimeOutMiliseconds>
  <EnableConsole>true</EnableConsole>
  <DisableWriteUnlessAdminPersistentFlag>true</DisableWriteUnlessAdminPersistentFlag>
  <DisableReadUnlessAdminPersistentFlag>false</DisableReadUnlessAdminPersistentFlag>
  <UseNetworkFinalCompression>false</UseNetworkFinalCompression>
  <EnableBSRProfiling>false</EnableBSRProfiling>
  <DisallowHeadless>false</DisallowHeadless>
  <AvatarsLocked>false</AvatarsLocked>
  <PropsLocked>false</PropsLocked>
  <WorldsLocked>true</WorldsLocked>
</Configuration>
```

## Laadbare configuratiebestanden

Je kunt wereld- en objectbestanden instellen om op aanvraag te laden.
Dit zijn de bestanden die je in de map `InitialResources` plaatst.

<Callout type="warn">
Let op dat de externe URL's die je gebruikt, moeten worden geleverd door een juiste server die chunking ondersteunt.
Elke moderne server zou dit moeten doen, maar eenvoudige lokale dev-servers mogelijk niet.
</Callout>

<Callout type="info" title="Iets uploaden">
Een manier om een nieuwe scène te testen is een scène in Unity te maken, deze te exporteren als een BEE-bestand. Upload dat BEE-bestand naar een server die HTTP ondersteunt, plaats vervolgens de URL in `CombinedURL` en haal het wachtwoord op uit `dontuploadmepassword.txt` en plaats het in `UnlockPassword`.
</Callout>

| Variabele        | Definitie |
|------------------|------------|
| `Mode`           | Modus van de configuratie: `0` = Gameobject, `1` = Scène. |
| `LoadedNetID`    | Een netwerk-ID: server houdt een lijst bij van netwerkapparaten die clients kunnen opvragen en spawnen met deze waarde als referentie. |
| `UnlockPassword` | Wachtwoord vereist om de bron te ontgrendelen of toegang te krijgen. |
| `CombinedURL`    | URL naar het `.BEE`-bestand |
| `IsLocalLoad`    | Geeft aan of de bron lokaal wordt geladen (`true`) of van het internet (`false`). |
| `PositionX`      | X-as wereldpositie van het object. |
| `PositionY`      | Y-as wereldpositie van het object. |
| `PositionZ`      | Z-as wereldpositie van het object. |
| `QuaternionX`    | X-component van de rotatie van het object (quaternion). |
| `QuaternionY`    | Y-component van de rotatie van het object (quaternion). |
| `QuaternionZ`    | Z-component van de rotatie van het object (quaternion). |
| `QuaternionW`    | W-component van de rotatie van het object (quaternion). |
| `ScaleX`         | X-as schaalfactor. |
| `ScaleY`         | Y-as schaalfactor. |
| `ScaleZ`         | Z-as schaalfactor. |
| `Persist`        | Als de speler die het object heeft gespawnd de server verlaat, blijft het object dan bestaan? |

### Voorbeeldconfiguratie

```xml title="BasisLoadableConfiguration-voorbeeld"
<BasisLoadableConfiguration>

    <!-- Mode of the configuration: Mode  0 = Game object, 1 = Scene -->
    <Mode>0</Mode>

    <!-- A Network ID: Server keeps a list of Network Objects that Clients can query and spawn with this value. -->
    <LoadedNetID></LoadedNetID>

    <!-- Unlock password -->
    <UnlockPassword>ffee4439eff</UnlockPassword>

    <!-- Combined URL link to BEE file -->
    <CombinedURL>https://www.example.com/2lkj23j.BEE</CombinedURL>

    <!-- Does this resource exist on the internet or in the client? -->
    <IsLocalLoad>false</IsLocalLoad>

    <!-- Position values -->
    <PositionX>0</PositionX>
    <PositionY>0</PositionY>
    <PositionZ>0</PositionZ>

    <!-- Quaternion values -->
    <QuaternionX>0</QuaternionX>
    <QuaternionY>0</QuaternionY>
    <QuaternionZ>0</QuaternionZ>
    <QuaternionW>1</QuaternionW>

    <!-- Scale values -->
    <ScaleX>1</ScaleX>
    <ScaleY>1</ScaleY>
    <ScaleZ>1</ScaleZ>

    <!--  If the player that spawned the object leaves the server does the object continue to exist?  -->
    <Persist>false</Persist>

</BasisLoadableConfiguration>
```
